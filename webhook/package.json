{
  "name": "jarvis-webhook",
  "version": "1.1.0",
  "description": "JARVIS Telegram webhook (Vercel serverless) + GH Actions pulse",
  "scripts": {
    "pulse": "node pulse.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "node-fetch": "^2.7.0"
  }
}
